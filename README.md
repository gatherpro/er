# 今日だけ自動運転

**朝に決めて、あとは自動で進む。**

今日一日を分岐まで含めて自動運転。決断は朝だけ、実行は通知がやる。

## コンセプト

一行で言うと：**今日だけ自動運転。朝の一文で分岐つき1日を確定し、通知が強制トランジションで前へ進める。**

### タグライン

1. 朝に決めて、あとは自動で進む。
2. 今日一日、分岐まで含めて自動運転。
3. 決断は朝だけ、実行は通知がやる。

## 体験原則（5箇条）

1. **今日の一本路線**：ビューは"今日のライン"のみ。迷う分岐は裏で自動判定。
2. **朝の一文→即確定**：テンプレ＋自然言語で分岐を定義。当日は基本"タップのみ"。
3. **日次ダイヤル**：Gentle / Coach / Boss の強度を朝に一発決定（通知強度・スヌーズ・圧縮率・強制遷移が連動）。
4. **強制トランジション**：終了・超過・割込みで機械的に遷移。優先規則＝締切＞移動＞対人＞机作業。
5. **安全と可逆性**：常時"緊急停止"、その日のダイヤルは常に弱めに戻せる。ログは透明で後から見直せる。

## 日次ダイヤル仕様

### 🌱 Gentle（ゆるやかに）

- 通知最小限
- スヌーズ2回まで
- 超過時は分割して後段へ
- 柔軟な調整が可能

### 💪 Coach（ほどよくプッシュ）

- 段階的なナッジ
- スヌーズ1回まで
- 超過は10%圧縮＋分割
- バランスの取れた強度

### ⚡ Boss（強制的に進める）

- スヌーズ不可
- チェックリスト通過まで遷移不可
- 超過は最大30%圧縮＋即切上げ
- 締切厳守モード

## 主な機能

- **朝の一文入力**：今日の予定を自然な言葉で入力
- **テンプレート選択**：よく使う1日パターンを素早く適用
- **日次ダイヤル**：今日の強度（Gentle/Coach/Boss）を選択
- **今日のタイムライン**：1日の流れを一覧表示
- **強制トランジション**：通知による自動タスク切り替え
- **優先度自動判定**：締切＞移動＞対人＞机作業
- **緊急停止**：いつでも全通知を停止可能
- **分岐設定**（今後実装予定）：条件による自動ルート選択

## 技術スタック

- **フロントエンド**: React Native + Expo
- **言語**: TypeScript
- **状態管理**: Zustand
- **通知**: Expo Notifications
- **ナビゲーション**: React Navigation
- **UI**: React Native Paper
- **ローカルストレージ**: AsyncStorage

## セットアップ

### 前提条件

- Node.js 18以上
- npm または yarn
- iOS: Xcode（Mac）
- Android: Android Studio

### インストール

```bash
# 依存関係のインストール
npm install

# または
yarn install
```

### 開発サーバー起動

```bash
# 開発サーバー起動
npm start

# iOSシミュレーター
npm run ios

# Androidエミュレーター
npm run android

# Web（開発用）
npm run web
```

### 物理デバイスでテスト

1. Expo Goアプリをインストール
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. 開発サーバーを起動
   ```bash
   npm start
   ```

3. QRコードをスキャン
   - iOS: カメラアプリで
   - Android: Expo Goアプリ内で

## プロジェクト構造

```
src/
├── screens/           # 画面コンポーネント
│   ├── MorningInputScreen.tsx      # 朝の一文入力
│   ├── DialSelectorScreen.tsx      # ダイヤル選択
│   ├── TodayTimelineScreen.tsx     # 今日のタイムライン
│   └── TaskDetailScreen.tsx        # タスク詳細
├── navigation/        # ナビゲーション設定
│   └── AppNavigator.tsx
├── stores/            # Zustand状態管理
│   └── dayStore.ts                 # メインストア
├── services/          # サービス層
│   └── notifications.ts            # 通知管理
├── types/             # TypeScript型定義
│   └── index.ts
└── utils/             # ユーティリティ
    ├── templates.ts                # テンプレート定義
    └── taskGenerator.ts            # タスク生成
```

## 使い方

### 基本フロー

1. **朝の入力**
   - アプリを起動
   - 今日の予定を一文で入力（または テンプレート選択）
   - 例：「午前は資料作成、午後はミーティング3つ、夕方から移動」

2. **ダイヤル選択**
   - 今日の強度（Gentle/Coach/Boss）を選択
   - 通知強度、スヌーズ回数、超過時の挙動が連動

3. **自動実行**
   - 「今日のライン」が表示される
   - 予定時刻に通知が届く
   - タスク完了タップまたは時間経過で自動遷移

4. **緊急停止**
   - 右下の赤いボタンで即座に停止
   - すべての通知が止まる
   - 再開ボタンで復帰可能

### テンプレート

以下のテンプレートが利用可能：

- **標準作業日**：メール→資料作成→MTG→まとめ
- **締切集中日**：締切案件に特化した構成
- **移動日**：移動時間を考慮したスケジュール
- **ミーティング多め**：MTG中心の1日
- **クリエイティブ集中**：ディープワーク重視

## 今後の実装予定

### Phase 1: MVP完成（現在）
- ✅ 基本UI・UX
- ✅ 朝の一文入力
- ✅ 日次ダイヤル
- ✅ タイムライン表示
- ✅ 通知による遷移
- ✅ 緊急停止

### Phase 2: 分岐とAI（次のステップ）
- ⬜ 自然言語処理（朝の一文→タスク自動生成）
- ⬜ 条件分岐の実装
- ⬜ 時刻チェック分岐
- ⬜ 完了状態分岐
- ⬜ 手動選択分岐

### Phase 3: 最適化
- ⬜ 実行ログと振り返り
- ⬜ 学習による時間見積もり改善
- ⬜ テンプレートのカスタマイズ
- ⬜ 音声入力対応
- ⬜ ウィジェット対応

## 対象ペルソナ

- 個人事業主・フリーランス
- クリエイター
- 割込みが多い人
- 締切が複数ある人
- 移動がある人

## やらないこと（アンチゴール）

- 長期プロジェクト管理
- 習慣のゲーミフィケーション
- 複雑なビュー・多画面構成
- 無制限スヌーズ
- 医療的助言

## 価値の約束

1. **決断の先送りゼロ**：決断は朝で完了、当日は実行に専念
2. **タスク切替の苦手を無力化**：強制トランジションで進む
3. **入力負荷ミニマム**：一文/音声→分岐テンプレ自動展開

## 差別化ポイント

- 「分岐を前提に設計された今日専用エンジン」
- 「日ごとダイヤル」で人の波に合わせて挙動が一括変化
- 「強制トランジション」を明示的に採用（可逆性・緊急停止付き）

## ライセンス

MIT

## 開発者

This project was created with Claude Code.
